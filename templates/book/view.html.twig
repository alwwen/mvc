{% extends "base.html.twig" %}

{% block title %}Library Page{% endblock %}

{% block body %}
    <h1>Library Page</h1>
    <h3>Alla böcker i biblioteket</h3>
    <ul>
        {% for book in books %}
            <li><a href="{{ path('book_by_id', {'id': book.getId}) }}">{{ book.getTitle|e }}</a> av {{ book.getAuthor|e }}. ISBN ({{ book.getIsbn|e }})</li>
            {# <img class="profile_picture" src="{{ asset('build/images/' ~ book.getImage|e) }}" alt=""> #}
        {% endfor %}
    </ul>
{% endblock %}